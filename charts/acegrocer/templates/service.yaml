apiVersion: v1
kind: Service
metadata:
  name: {{ include "acegrocer.fullname" . }}
  labels:
    {{- include "acegrocer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "acegrocer.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
